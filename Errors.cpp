#include "stdafx.h"
#include "ErrorCodes.h"

namespace specFuncs {
	std::vector<char *> errors{
		"must be a real integer between 0 and 128 (inclusive)",
		"must be a real non-negative value",
		"overflow",
		"must be a positive real number",
		"must be a real between -1 and 1",
		"must be a real number between 0.0 and 128.0 (inclusive)",
		"must be a non-negative real",
		"must be real",
		"must be a real integer"
	};
}